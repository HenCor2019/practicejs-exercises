const rotateList = require('./rotateList')

/*
 test('Empty parameters to be -1', () => {
   const response = rotateList()
   expect(response).toBe(-1)
 })
*/

/*
 test('First parameter is not an Array and second is number lower than 0 to be -1', () => {
   const response = rotateList(1000, [])
   expect(response).toBe(-1)
 })
*/

/*
 test('3 rotations in [6,1,2,3] to be [1,2,3,6]', () => {
   const response = rotateList([6,1,2,3], 3)
   expect(response).toEqual(expect.arrayContaining([1,2,3,6]))
 })
*/

/*
 test('5 rotations in [7,1,4,1] to be [1,7,1,4]', () => {
   const response = rotateList([7,1,4,1], 5)
   expect(response).toEqual(expect.arrayContaining([1,7,1,4]))
 })
*/
