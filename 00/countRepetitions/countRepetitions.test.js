const countRepetitions = require('./coutRepetitions')

/*
 test('Empty parameters to be -1', () => {
   const response = countRepetitions()
   expect(response).toBe(-1)
 })
*/

/*
 test('First parameter is not an array and second parameter is not a number to be -1', () => {
   const response = countRepetitions('this is no an array', [] )
   expect(response).toBe(-1)
 })
*/

/*
 test('Count all 3 in [4,1,3,6,3,1,2,3,7,5,2] to be 3', () => {
   const response = countRepetitions([4,1,3,6,3,1,2,3,7,5,2], 3)
   expect(response).toBe(3)
 })
*/

/*
 test('Count all 1 in [7,1,6,21,1,7,1,2,1,9,1] to be 5', () => {
   const response = countRepetitions([7,1,6,21,1,7,1,2,1,9,1], 1)
   expect(response).toBe(5)
 })
*/

/*
 test('Count all 4 in [0,5,1,3,7,2,89,65,32,42] to be 0', () => {
   const response = countRepetitions([0,5,1,3,7,2,89,65,32,42], 4)
   expect(response).toBe(0)
 })
*/
